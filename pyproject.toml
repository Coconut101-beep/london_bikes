[project]
name = "london-bikes-dashboard"
version = "0.1.0"
description = "Dash dashboard for exploring London bike hires and predicting demand from weather"
requires-python = ">=3.10"
dependencies = [
    "dash",
    "gunicorn",
    "pandas",
    "plotly",
    "requests",
]
